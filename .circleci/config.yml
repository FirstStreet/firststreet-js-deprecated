version: 2.1
orbs:
  codecov: codecov/codecov@1.0.4
jobs:
  build:
    working_directory: ~/floodiq-javascript
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run:
          name: update-npm
          command: 'sudo npm install -g npm@latest'
      - restore_cache: # restrore dependency cache
          key: dependency-cache-{{ checksum "package.json" ***REMOVED******REMOVED***
      - run:
          name: install-npm-wee
          command: npm install
      - save_cache: # save dependency cache
          key: dependency-cache-{{ checksum "package.json" ***REMOVED******REMOVED***
          paths:
            - ./node_modules
  test:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: # run tests
          name: test
          command: npm test
      - run: # run code coverage
          name: code-coverage
          command: npm test --coverage
      - codecov/upload:
          token: 11496673-9497-42a3-b3a4-3646b53e7027
          file: coverage/lcov.info
      # - store_artifacts: # save test results
      #     path: test-results.xml
      # - store_artifacts: # save coverage results
      #     path: coverage
      # - store_test_results:
      #     path: test-results.xml
workflows:
  version: 2
  "Build, Test":
    jobs:
      - build
      - test

